# Copy to application-local.properties in same directory (src/main/resources/).
# File application-local.properties is gitignored — use to override local dev settings.
# Loaded automatically via spring.config.import in application.properties (optional).

# For local dev with SQL Server on host machine (not Docker):
# spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=autohub;encrypt=false;trustServerCertificate=true
# spring.datasource.password=YourLocalPassword

# MAIL_USERNAME=
# MAIL_PASSWORD=
# GEMINI_API_KEY=
